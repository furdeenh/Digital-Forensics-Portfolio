# HTTP Analysis Findings

- Multiple HTTP GET requests retrieved text-based files.
- Encoded base64 strings were identified within TCP streams.
- Plaintext PII was observed in exported HTTP objects.
- Directory indexing exposed internal server file structure.
